{{- if .Values.global.zenith.domain }}
# =============================================================================
# Monitoring IngressRoutes — REMOVED (Phase 7: API-as-Proxy)
# =============================================================================
# Grafana and Prometheus are now accessed through the Zenith API proxy at:
#   /api/v1/admin/proxy/grafana/*
#   (Prometheus is queried internally by the observability handler)
#
# The IngressRoutes, Certificates, and BasicAuth middleware that were here
# have been intentionally removed to reduce public attack surface.
# All admin-facing services are accessed through JWT-authenticated API proxy.
#
# To restore public access (NOT recommended), see git history for this file.
# =============================================================================
{{- end }}
