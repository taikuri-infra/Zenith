# =============================================================================
# Traefik IngressRoutes for Zenith Platform
# Uses traefik.io/v1alpha1 IngressRoute CRD (NOT kubernetes.io/v1 Ingress)
# =============================================================================

# -----------------------------------------------------------------------------
# freezenith.com / www.freezenith.com -> zenith-landing:3000
# -----------------------------------------------------------------------------
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: freezenith-landing
  namespace: zenith-platform
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`freezenith.com`) || Host(`www.freezenith.com`)
      kind: Rule
      services:
        - name: zenith-landing
          port: 3000
  tls:
    secretName: freezenith-tls
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: freezenith-landing-redirect
  namespace: zenith-platform
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`freezenith.com`) || Host(`www.freezenith.com`)
      kind: Rule
      services:
        - name: zenith-landing
          port: 3000
      middlewares:
        - name: redirect-to-https
---
# -----------------------------------------------------------------------------
# demo-ms.freezenith.com -> zenith-mc-demo:3100 (MC showroom)
# -----------------------------------------------------------------------------
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: freezenith-demo-ms
  namespace: zenith-platform
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`demo-ms.freezenith.com`)
      kind: Rule
      services:
        - name: zenith-mc-demo
          port: 3100
  tls:
    secretName: freezenith-tls
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: freezenith-demo-ms-redirect
  namespace: zenith-platform
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`demo-ms.freezenith.com`)
      kind: Rule
      services:
        - name: zenith-mc-demo
          port: 3100
      middlewares:
        - name: redirect-to-https
---
# -----------------------------------------------------------------------------
# demo-cloud.freezenith.com -> zenith-web-demo:3000 (Web showroom)
# -----------------------------------------------------------------------------
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: freezenith-demo-cloud
  namespace: zenith-platform
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`demo-cloud.freezenith.com`)
      kind: Rule
      services:
        - name: zenith-web-demo
          port: 3000
  tls:
    secretName: freezenith-tls
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: freezenith-demo-cloud-redirect
  namespace: zenith-platform
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`demo-cloud.freezenith.com`)
      kind: Rule
      services:
        - name: zenith-web-demo
          port: 3000
      middlewares:
        - name: redirect-to-https
---
# -----------------------------------------------------------------------------
# api.freezenith.com -> zenith-api:8080
# -----------------------------------------------------------------------------
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: freezenith-api
  namespace: zenith-platform
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`api.freezenith.com`)
      kind: Rule
      services:
        - name: zenith-api
          port: 8080
  tls:
    secretName: freezenith-tls
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: freezenith-api-redirect
  namespace: zenith-platform
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`api.freezenith.com`)
      kind: Rule
      services:
        - name: zenith-api
          port: 8080
      middlewares:
        - name: redirect-to-https
---
# HTTPS redirect middleware (zenith-platform namespace)
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: redirect-to-https
  namespace: zenith-platform
spec:
  redirectScheme:
    scheme: https
    permanent: true
---
# =============================================================================
# embermind.app customer domains (zenith-embermind namespace)
# =============================================================================

# -----------------------------------------------------------------------------
# ms.embermind.app -> zenith-mc:3100 (customer Mission Control)
# -----------------------------------------------------------------------------
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: embermind-mission
  namespace: zenith-embermind
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`ms.embermind.app`)
      kind: Rule
      services:
        - name: zenith-mc
          port: 3100
  tls:
    secretName: embermind-tls
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: embermind-mission-redirect
  namespace: zenith-embermind
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`ms.embermind.app`)
      kind: Rule
      services:
        - name: zenith-mc
          port: 3100
      middlewares:
        - name: redirect-to-https
---
# -----------------------------------------------------------------------------
# cloud.embermind.app -> zenith-web:3000 (customer cloud platform)
# -----------------------------------------------------------------------------
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: embermind-cloud
  namespace: zenith-embermind
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`cloud.embermind.app`)
      kind: Rule
      services:
        - name: zenith-web
          port: 3000
  tls:
    secretName: embermind-tls
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: embermind-cloud-redirect
  namespace: zenith-embermind
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`cloud.embermind.app`)
      kind: Rule
      services:
        - name: zenith-web
          port: 3000
      middlewares:
        - name: redirect-to-https
---
# HTTPS redirect middleware (zenith-embermind namespace)
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: redirect-to-https
  namespace: zenith-embermind
spec:
  redirectScheme:
    scheme: https
    permanent: true
