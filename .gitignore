node_modules/
.next/
dist/
.turbo/
*.tsbuildinfo
.env
.env.local
.env.production
.DS_Store
package-lock.json

# CI secrets (act)
.secrets

# Terraform
*.tfvars
*.tfstate
*.tfstate.backup
*.tfstate.*.backup
.terraform/
.terraform.lock.hcl
.urls

# Helm dependencies (downloaded via helm dep up)
**/charts/*.tgz

# Go binary
services/api/server

# Python cache
__pycache__/

# Local AI tooling / private business strategy (never publish to the public repo)
.claude/
freezenith-strategy.md
