{
  "name": "zenith",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "dev:mc": "turbo dev --filter=@zenith/mission-control",
    "dev:web": "turbo dev --filter=@zenith/web",
    "build:mc": "turbo build --filter=@zenith/mission-control",
    "build:web": "turbo build --filter=@zenith/web"
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@10.29.3"
}
