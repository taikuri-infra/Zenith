{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "scope-enum": [1, "always", [
      "api", "web", "admin", "landing", "mc",
      "cli", "operator", "helm", "terraform",
      "ci", "infra", "deps", "installer"
    ]],
    "subject-case": [1, "always", "lower-case"]
  }
}
